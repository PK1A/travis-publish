language: node_js
node_js:
  - "0.10"

before_install:
 - npm install --quiet -g grunt-cli

script:
 - grunt

after_success:
  if [ $TRAVIS_PULL_REQUEST = "false" ]; then
    echo "Build from ${TRAVIS_BRANCH}"
  fi
